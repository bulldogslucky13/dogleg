# Supabase project config. Only the pieces this repo actually depends on are
# declared here — everything else takes CLI defaults.
#
# Note that the Auth settings (site_url, redirect allow-list, SMTP) are managed
# in the dashboard, not here: this file governs local `supabase start/serve` and
# function deploys, and pushing auth config from it would need an explicit
# `supabase config push`. See CLAUDE.md.
#
# The one exception is the Auth email templates, which are no longer
# dashboard-only: they are generated into supabase/templates/ from
# supabase/functions/_shared/auth-emails.ts, and CI pushes them on every merge
# to main (the email-templates job in .github/workflows/deploy.yml). Edit them
# there, not in the dashboard — a dashboard edit survives only until the next
# deploy, which will silently overwrite it.

project_id = "cphmpypnvopoylwxsrlk"

# The referee. Anonymous players post scores, and the function authenticates
# submissions itself (device secret, then a full engine replay), so a gateway
# JWT check would only lock out the people it exists to serve.
[functions.submit-round]
verify_jwt = false

# Account linking. Requires a real signed-in user — the function re-checks the
# token with getUser() and 401s, so the gateway check is defence in depth.
[functions.link-account]
verify_jwt = true

# Anonymous identity minting. Called before a player has any credentials at
# all — a JWT gate would defeat its purpose. Rate limits itself per IP.
[functions.mint-player]
verify_jwt = false

# Naming an anonymous identity without posting a card. Authenticates on the
# player's own device secret, exactly as submit-round does for the same write
# — a gateway JWT check would lock out every player it exists to serve.
[functions.claim-name]
verify_jwt = false

# Inbound mail webhook. The caller is Resend, which can't mint Supabase JWTs;
# the function authenticates every request itself via the svix signature
# (RESEND_WEBHOOK_SECRET) and fails closed when the secret is unset.
[functions.receive-email]
verify_jwt = false
